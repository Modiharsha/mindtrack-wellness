{
  "name": "mindtrack-wellness-system",
  "version": "1.0.0",
  "description": "Student Mental Health & Wellness Monitoring System",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build:all": "npm install --prefix server && npm install --prefix client && cd server && npx prisma generate && npx prisma db push --accept-data-loss && cd .. && npm run build --prefix client",
    "build": "npm run build:all",
    "start": "cd server && npx tsx src/index.ts",
    "db:push": "npm run db:push --prefix server",
    "db:seed": "npm run db:seed --prefix server",
    "test": "npm run test --prefix server",
    "postinstall": "cd server && npx prisma generate"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
